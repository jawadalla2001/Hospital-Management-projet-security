# Hospital Management System Environment Configuration

# Application Settings
NODE_ENV=development    # Change to 'production' for production environment
PORT=3000               # The port the application will run on
BASE_URL=http://localhost:3000  # Change to your production URL when deployed

# Security
SESSION_SECRET=change-this-to-a-long-random-string  # Used for session encryption

# Email Configuration for Verification Emails
EMAIL_USERNAME=your-email@gmail.com     # Email account for sending notifications
EMAIL_PASSWORD=your-app-password        # App password for the email account
EMAIL_FROM=Hospital Management <your-email@gmail.com>

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=nodelogin

# INSTRUCTIONS:
# 1. Copy this file to ".env" in the project root
# 2. Replace the placeholder values with your actual credentials
# 3. Keep the .env file secure and never commit it to version control
# 4. In production, use a secrets management solution if possible